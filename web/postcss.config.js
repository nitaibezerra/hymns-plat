/**
 * Marco 4.B — Ciclo 4B.1.
 *
 * PostCSS config para Tailwind 4 (plugin `@tailwindcss/postcss`). Esse
 * arquivo é o que SvelteKit/Vite vai procurar pra processar `@import
 * "tailwindcss"` dentro de `src/app.css`.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
